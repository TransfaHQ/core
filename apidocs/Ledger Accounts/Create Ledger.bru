meta {
  name: Create Ledger
  type: http
  seq: 2
}

post {
  url: {{APP_URL}}/{{APP_VERSION}}/ledger_accounts
  body: json
  auth: inherit
}

body:json {
  {
    "ledgerId": "{{LEDGER_ID}}",
    "name": "Credit Primary Account Client 3",
    "currency": "USD",
    "normalBalance": "credit",
    "currencyExponent": 1,
    "metadata": {"test": "test1"}
  }
}

script:post-response {
  bru.setEnvVar('LEDGER_ACCOUNT_ID',res.body.id)
}

settings {
  encodeUrl: true
}
