meta {
  name: Update Ledger Account
  type: http
  seq: 5
}

patch {
  url: {{APP_URL}}/{{APP_VERSION}}/ledger_accounts/:id
  body: json
  auth: inherit
}

params:path {
  id: {{LEDGER_ACCOUNT_ID}}
}

body:json {
  {
    "name": "Ledger 2",
    "description": "Ledger 2 description",
    "metadata": {"core": "test"}
  }
}

settings {
  encodeUrl: true
}
