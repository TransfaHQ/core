meta {
  name: Create Ledger
  type: http
  seq: 2
}

post {
  url: {{APP_URL}}/{{APP_VERSION}}/ledgers/
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Ledger 4",
    "metadata": {"key": "key", "value": "value"}
  }
}

script:post-response {
  bru.setEnvVar('LEDGER_ACCOUNT_ID',res.body.id)
}

settings {
  encodeUrl: true
}
